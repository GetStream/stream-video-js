---
sidebar_position: 1
title: Android
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

## Setup

1. Install the following modules to work with Firebase Cloud Messaging (FCM).

<Tabs groupId="package-manager">
  <TabItem value="Yarn" default>

```bash
  # Install & setup the app module
  yarn add @react-native-firebase/app
  # Install the messaging module
  yarn add @react-native-firebase/messaging
```

  </TabItem>
  <TabItem value="npm">

```bash
  # Install & setup the app module
  npm install react-native-voip-push-notification
  # Install the messaging module
  npm install --save @react-native-firebase/messaging
```
  </TabItem>
</Tabs>

2. [Add credentials to your Android app and configure Firebase](https://rnfirebase.io/#2-android-setup).
