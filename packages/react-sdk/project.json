{
  "name": "@stream-io/video-react-sdk",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "dryRun": false,
        "preset": "angular",
        "preid": "alpha",
        "releaseAs": "prerelease",
        "trackDeps": true,
        "push": true,
        "skipCommitTypes": ["chore", "ci"],
        "postTargets": ["@stream-io/video-react-sdk:github", "@stream-io/video-react-sdk:npm"]
      }
    },
    "github": {
      "executor": "@jscutlery/semver:github",
      "options": {
        "tag": "${tag}",
        "notes": "${notes}"
      }
    },
    "npm": {
      "executor": "ngx-deploy-npm:deploy",
      "options": {
        "access": "public",
        "noBuild": true,
        "dryRun": true,
        "distFolderPath": "packages/react-sdk"
      }
    }
  }
}
