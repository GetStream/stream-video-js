{
  "name": "@stream-io/video-components-react",
  "packageManager": "yarn@3.2.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "start": "tsc --project tsconfig.json --watch",
    "start:styles": "sass --watch index.scss ./dist/css/styles.css",
    "build": "tsc --project tsconfig.production.json && yarn build:styles",
    "build:styles": "sass index.scss ./dist/css/styles.css"
  },
  "dependencies": {
    "@stream-io/video-client": "workspace:^",
    "@stream-io/video-client-sfu": "workspace:^"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/rimraf": "^3.0.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "rimraf": "^3.0.2",
    "sass": "^1.55.0",
    "typescript": "^4.8.3"
  }
}
