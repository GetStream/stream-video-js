export const EVENT_MAP = {
  'health.check': true,

  'call.created': true,
  'call.accepted': true,
  'call.rejected': true,
  'call.cancelled': true,

  // local events
  'connection.changed': true,
  'connection.recovered': true,
  'transport.changed': true,
};
